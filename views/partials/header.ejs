<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Habit Tracker</title>
    <link rel="stylesheet" href="/styles.css">
    
    <style>
        body { font-family: sans-serif; max-width: 900px; margin: 0 auto; padding: 20px; background-color: #f4f7f6; }
        .habit-card { background: white; border: 1px solid #ddd; padding: 20px; margin-bottom: 15px; border-radius: 12px; box-shadow: 0 4px 6px rgba(0,0,0,0.05); }
        .streak { font-weight: bold; color: green; }
        h1 { text-align: center; color: #333; margin-bottom: 30px; }
        .status-board { display: flex; gap: 20px; margin-bottom: 30px; }
        .badges-box { flex: 1; border: 1px solid #f1c40f; padding: 15px; border-radius: 10px; background: #fffbe6; }
        .tips-box { flex: 1; border: 1px solid #3498db; padding: 15px; border-radius: 10px; background: #e6f7ff; }
        .btn-action { cursor: pointer; border: none; padding: 8px 12px; border-radius: 5px; margin-right: 5px; }
    </style>
</head>
<body>
    <h1>🚀 Мій Трекер Звичок</h1>